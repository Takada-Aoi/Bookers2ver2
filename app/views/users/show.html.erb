<%= render 'users/info',user:@user %>
<%= render 'books/new',book:@newbook %>

<tboby>
    <% @books.each do |book| %>
    <tr>
      <td><%=attachment_image_tag @user, :profile_image,:fill,40,40 %></td>
      <td><%=book.title %></td>
      <td><%=book.body%></td>
    </tr>
    <% end %>
  </tboby>
</table>